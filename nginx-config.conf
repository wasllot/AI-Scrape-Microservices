# /etc/nginx/sites-available/api.reinaldotineo.online

server {
    server_name api.reinaldotineo.online;

    location / {
        # Proxy to Traefik (running on localhost:8090 via Docker)
        proxy_pass http://127.0.0.1:8090;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Logs
    access_log /var/log/nginx/api_access.log;
    error_log /var/log/nginx/api_error.log;
}

# -----------------------------------------------------------------
# INSTRUCCIONES PARA ACTIVAR:
# 1. Copia este archivo: sudo cp nginx-config.conf /etc/nginx/sites-available/api.reinaldotineo.online
# 2. Enlázalo: sudo ln -s /etc/nginx/sites-available/api.reinaldotineo.online /etc/nginx/sites-enabled/
# 3. Test: sudo nginx -t
# 4. Reload: sudo systemctl reload nginx
# 5. SSL: sudo certbot --nginx -d api.reinaldotineo.online
# -----------------------------------------------------------------
